/**
 * UNUSED FILE - NOT REFERENCED ANYWHERE IN CODEBASE
 * This service was created but never integrated.
 * adminModel methods are called directly in adminController instead.
 * 
 * Date marked unused: January 4, 2026
 * Can be safely deleted if not needed in future.
 */

/*
const adminModel = require('../models/adminModel');

const fetchAllAdmins = async () => {
    return await adminModel.getAllAdmins();
};

module.exports = { fetchAllAdmins };
*/