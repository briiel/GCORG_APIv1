#!/usr/bin/env node
// UNUSED SCRIPT — reminder system disabled (reminderService.js empty, mailer.js commented out)
// To enable: implement reminderService.js, uncomment mailer.js, and schedule via cron at 8AM

// Reminder sweep script disabled
process.exit(0);
